class Loot {
     /*
        Constructor
        Entrada:
        - mesh:     Mesh del Loot
        - health:   HP que le va a dar al jugador
    */
    constructor(mesh, health) {
        this.mesh = mesh;
        this.health = health;
    }
}